{
  "name": "advancy-ai-score-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create advancy-ai-results",
    "db:migrate": "wrangler d1 execute advancy-ai-results --remote --file=./schema.sql",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
